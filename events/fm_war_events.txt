namespace = fm_war_good 

fm_war_good.1 = {
	placement = root

	title = fm_war_good.1.t
	desc = fm_war_good.1.desc
	flavor = fm_war_good.1.f

	duration = 1

	cooldown = {
		days = 120
	}
	
	event_image = {
		video = "gfx/event_pictures/middleeast_battlefield_trenches.bk2"
	}

	option = {
		name = fm_war_good.1.a
		default_option = yes
		local_var:current_war = {
			add_war_war_support = {
				target = ROOT
				value = 5
			}			
		}
		add_modifier = { name = war_support_letters_home months = 2 }
		ai_chance = {
			base = 0
			modifier = { trigger = { wsr_check_debt_bad = yes } add = 1000 }
		}
	}

	option = {
		name = fm_war_good.1.b
		default_option = yes
		local_var:current_war = {
			add_war_war_support = {
				target = ROOT
				value = 10
			}			`
		}
		add_modifier = { name = war_support_letters_wide months = 6 }
		add_treasury = wsr_money_amount_multiplier_small
		add_loyalists = {
			value = 0.02
			pop_type = soldiers
		}
		add_loyalists = {
			value = 0.01
			pop_type = officers
		}
		ai_chance = {
			base = 0
			modifier = { trigger = { wsr_check_debt_normal = yes } add = 100 }
			modifier = { trigger = { wsr_check_war_support_bad = yes } add = 50 }
		}
	}
}